<?xml version="1.0" encoding="UTF-8" ?>
<class name="Summator" inherits="Object" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:noNamespaceSchemaLocation="https://raw.githubusercontent.com/godotengine/godot/master/doc/class.xsd">
	<brief_description>
		A [i]classic[/i] code example for a [b]GDExtension[/b]
	</brief_description>
	<description>
		The class must inherit from a Godot built in class (like [Object], [Node], [Sprite2D], or [Resource]). Godot does not support multiple inheritance for GDExtension classes. Why the #@# does the editor documentation allow csharp code blocks if it never displays them? [br] [br] Example Usage: [codeblocks][gdscript]var sum = Summator.new()
 sum.add(5)
 sum.add(7)
 var total = sum.get_total()
 # prints 12
 print(total)
 [/gdscript][csharp]GodotObject sum = ClassDB.Instantiate( "Summator" ).As&lt;GodotObject&gt;();
 sum.Call( "add" ,5);
 sum.Call( "add" ,7);
 int total = sum.Call( "get_total" ).As&lt; int &gt;();
 // prints 12
 GD.Print(total);
 [/csharp][/codeblocks] [br] [br] Check Status Example: [codeblock lang=gdscript]if total &gt;= sum.SUM_GOOD:
 print("Life is good")
 elif total &gt;= sum.SUM_OKAY:
 print("Could be doing better")
 elif total &gt;= sum.SUM_REQUIRED:
 print("At least you making rent")
 else:
 print("Your broke")
 [/codeblock]
	</description>
	<tutorials>
	</tutorials>
	<methods>
		<method name="add">
			<return type="void" />
			<param index="0" name="value" type="int" />
			<description>
				This function simply adds the integer value of the argument to the current total [br] [br]
			</description>
		</method>
		<method name="get_total" qualifiers="const">
			<return type="int" />
			<description>
				This function returns the current total, which is the sum of all the integers the summator added together. [br] [br]
			</description>
		</method>
		<method name="reset">
			<return type="void" />
			<description>
				resets the current total to zero
			</description>
		</method>
	</methods>
	<signals>
		<signal name="sum_changed">
			<param index="0" name="sum" type="int" />
			<description>
				This [b]signal[/b] , is [i]emitted[/i] when the sum changes whether after adding a new integer or when resetting the total back to zero.[br][br][b]Note:[/b] “You're on Earth. There's no cure for that.” ― Samuel Beckett
			</description>
		</signal>
		<signal name="sum_reset">
			<description>
				This signal is emitted when the total is reset to zero[br][br][b]Note:[/b] Gogo: 'We always find something, eh Didi, to give us the impression we exist?"[br][br][b]Warning:[/b] I'm making this up as I go along
			</description>
		</signal>
	</signals>
	<constants>
		<constant name="SUM_REQUIRED" value="50">
			The minimum total that is required to meet expenses
		</constant>
		<constant name="SUM_OKAY" value="100">
			Meeting expenses with a little extra
		</constant>
		<constant name="SUM_GOOD" value="200">
			Things are going good, well into the black
		</constant>
	</constants>
</class>
